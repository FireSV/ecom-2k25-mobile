class Address {
  int id;
  String street;
  String city;
  String state;
  String postalCode;
  String country;
  int userId;

  Address(this.id, this.street, this.city, this.state, this.postalCode,
      this.country, this.userId);
}
